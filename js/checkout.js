const selection=JSON.parse(localStorage.cinebookSelection||'null');if(!selection?.seats?.length)location.href='index.html';
(async()=>{try{const movie=await getMovie(selection.movieId),total=movie.price*selection.seats.length;document.querySelector('#checkout-summary').innerHTML=`<p class="eyebrow">ORDER SUMMARY</p><h2>${movie.title}</h2><p class="muted">${selection.theater}</p><p class="muted">${selection.date} · ${selection.time}</p><hr><p class="summary-row"><span>Seats</span><strong>${selection.seats.join(', ')}</strong></p><p class="summary-row"><span>${selection.seats.length} × ${formatPrice(movie.price)}</span><strong>${formatPrice(total)}</strong></p><p class="total"><span>Total</span><strong>${formatPrice(total)}</strong></p>`;document.querySelector('#checkout-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target),response=await fetch('/api/bookings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({movieId:selection.movieId,theatre:selection.theater,showDate:selection.date,showtime:selection.time,seats:selection.seats,customerName:f.get('name'),email:f.get('email'),phone:f.get('phone'),paymentMethod:f.get('payment')})}),data=await response.json();if(!response.ok)return alert(data.message||'Booking could not be completed.');localStorage.cinebookLatest=JSON.stringify({...data.booking,movie,date:selection.date,time:selection.time,total:data.booking.totalAmount});localStorage.cinebookLastEmail=f.get('email');location.href='confirmation.html';};}catch{document.querySelector('#checkout-summary').innerHTML='<p class="empty">Checkout could not be loaded.</p>';}})();
